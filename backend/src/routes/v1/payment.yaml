components:
  schemas:
    CreateCheckoutSessionResponse:
      type: object
      required:
        - url
      properties:
        url:
          type: string
          description: The URL to redirect the user to for payment
